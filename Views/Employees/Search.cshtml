@model IEnumerable<MVC_First.Models.Employee>

@{
    ViewData["Title"] = "Search Employee";
}
<h2>Search Results</h2>

<form asp-action="Search" method="get">
    <input type="text" name="name" placeholder="Enter name" class="form-control mb-2" autocomplete="off" />
    <button type="submit" class="btn btn-primary">Search</button>
</form>

@if (Model != null)

{   
    if (Model.Any()){
        <table class="table table-striped table-hover table-bordered mt-3">
            <thead class="table-dark">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.age)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.dob)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.city)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var emp in Model)
                {
                    <tr>
                        <td class="col-3">@emp.name</td>
                        <td class="col-2">@emp.age</td>
                        <td class="col-2">@emp.dob.ToShortDateString()</td>
                        <td class ="col-2">@emp.city</td>
                        <td class="col-3">
                            <a asp-action="Edit" asp-route-id="@emp.id" class="btn btn-sm btn-primary "><i class="bi bi-pencil-square "></i>Edit</a>
                            <a asp-action="Details" asp-route-id="@emp.id" class="btn btn-sm btn-info text-white"><i class="bi bi-eye"></i>Details</a>
                            <a asp-action="Delete" asp-route-id="@emp.id" class="btn btn-sm btn-danger"><i class="bi bi-trash"></i>Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p class="text-danger">No employees found.</p>
    }
}